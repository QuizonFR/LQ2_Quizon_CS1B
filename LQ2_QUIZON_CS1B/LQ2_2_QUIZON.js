let dogBreed = prompt("Enter Dog Breed (e.g. Askal): "); // The user will input the dog's breed
// The user will input how many times it will print the user's chosen dog breed
let printCount = Number(prompt("Number of Times to Print the Breed: "));

for (i = 0; i < printCount; i++) {
    console.log(dogBreed); // It will print the times of the user's print_count
}